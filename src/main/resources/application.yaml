spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/university
    username: postgres
    password: 1234
    driver-class-name: org.postgresql.Driver
  mvc:
    hiddenmethod:
      filter:
        enabled: true
  jpa:
    properties:
      hibernate:
        show_sql: true
        format_sql: true
#        hbm2ddl:
#          auto: validate
logging:
  file:
    name: task10.log
    path: /
  level:
    root: INFO
